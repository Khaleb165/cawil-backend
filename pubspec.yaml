name: cawil_backend
description: CaWil - Bus ticket booking backend (Dart Frog + PostgreSQL)

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  postgres: ^3.5.0
  crypto: ^3.0.3
  bcrypt: ^1.2.0
  http_parser: ^4.0.2
  json_annotation: ^4.8.0
  jose_plus: ^0.4.3
  pdf: ^3.10.0
  qr: ^3.0.0
  uuid: ^4.0.0
  mime: ^1.0.4
  mailer: ^7.2.0

dev_dependencies:
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  test: ^1.24.0
